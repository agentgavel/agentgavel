# Adapter contract

`adapter.proto` is the source of truth for the AgentGavel adapter wire contract
(RFC 0001 section 5.3). Default runtime transport is JSON-RPC 2.0 over stdio
(ADR 002); message shapes mirror this schema.
